import { FilterTargetGroupsPipe } from './filter-target-groups.pipe';

describe('FilterTargetGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTargetGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
